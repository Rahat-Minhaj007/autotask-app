import React from "react";
import {Text, View} from "react-native";

const Tasks = () => {
    return (
        <View className="flex-1 bg-primary justify-center items-center">
            <Text className="text-5xl text-white font-bold">Tasks</Text>
        </View>
    );
};

export default Tasks;